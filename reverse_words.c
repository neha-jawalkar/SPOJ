#include <stdio.h>
#define INPUT_BUF 26
#define MAX_ITERATIONS 10
char buf[MAX_ITERATIONS][INPUT_BUF]
int main()
{
	int t, i = 0;
	scanf("%d",t);
	for(i=0;i<t;i++)
	{
		char c;
		while(c = getc(stdin) != '\n')
	}
}